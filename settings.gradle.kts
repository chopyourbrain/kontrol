pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "kontrol"
include(":kontrol")
include(":sample:androidApp")
include(":sample:shared")
